{
  "enabled": {
    "type": "bool",
    "default": true,
    "description": "启用图片注入审计",
    "hint": "关闭后插件不会记录任何 LLM 请求图片统计。"
  },
  "warn_image_limit": {
    "type": "int",
    "default": 30,
    "description": "图片数量告警阈值",
    "hint": "当统计到的图片总数大于等于该值时使用 warning 日志；设为 0 可关闭阈值告警。"
  },
  "include_context_images": {
    "type": "bool",
    "default": true,
    "description": "统计会话历史中的图片",
    "hint": "OpenAI 兼容接口的 30 张图片限制通常按整段 messages/conversation 计算，因此建议保持开启。"
  },
  "remove_invalid_images": {
    "type": "bool",
    "default": true,
    "description": "发送模型前移除坏图片",
    "hint": "开启后会移除空引用、解码失败、文件不存在、明显不是图片的本地/内联图片，避免所有模型请求都因为同一张坏图 400。"
  },
  "remove_invalid_context_images": {
    "type": "bool",
    "default": true,
    "description": "同时清理历史上下文里的坏图片",
    "hint": "建议开启。坏图片常常来自历史上下文回带；不清理 contexts 的话 fallback 模型也会继续失败。"
  },
  "validate_remote_images": {
    "type": "bool",
    "default": false,
    "description": "联网验证远程图片 URL",
    "hint": "默认关闭以避免增加延迟。开启后会请求远程 URL 的前一段内容，若返回错误页或非图片内容则移除。"
  },
  "remote_validation_timeout_seconds": {
    "type": "int",
    "default": 5,
    "description": "远程图片验证超时时间",
    "hint": "仅在开启联网验证远程图片 URL 时生效。"
  },
  "remote_validation_max_bytes": {
    "type": "int",
    "default": 65536,
    "description": "远程图片验证读取字节数",
    "hint": "只读取开头一小段用于判断文件头，避免下载完整大图。"
  },
  "min_image_bytes": {
    "type": "int",
    "default": 16,
    "description": "最小图片字节数",
    "hint": "小于该字节数的本地/内联图片会被视为坏图。"
  },
  "removed_detail_limit": {
    "type": "int",
    "default": 8,
    "description": "坏图移除日志明细数量",
    "hint": "一次请求移除很多坏图时，日志只展示前若干条。"
  },
  "log_agent_context": {
    "type": "bool",
    "default": true,
    "description": "记录 Agent 开始前的上下文图片数",
    "hint": "该日志发生在 AgentRunner 创建后、上下文压缩前，可帮助确认历史图片是否已经进入本轮上下文。"
  },
  "track_history_carryover": {
    "type": "bool",
    "default": true,
    "description": "识别上一轮图片是否被带入本轮上下文",
    "hint": "插件会根据图片内容和本地路径生成指纹，尽量判断当前上下文里的图片是否来自上一轮请求。"
  },
  "log_tool_result_images": {
    "type": "bool",
    "default": true,
    "description": "记录工具调用结果中的图片",
    "hint": "如果工具返回图片内容，本插件会在工具结果 hook 中单独记录来源。"
  },
  "log_zero_image_requests": {
    "type": "bool",
    "default": false,
    "description": "记录没有图片的请求",
    "hint": "排查期间通常不需要打开；打开后日志会更密集。"
  },
  "include_details": {
    "type": "bool",
    "default": true,
    "description": "输出图片明细",
    "hint": "明细会包含来源、位置和截断后的路径/URL；data URI 只输出长度和哈希，不输出完整 base64。"
  },
  "detail_limit": {
    "type": "int",
    "default": 12,
    "description": "单条日志最多输出的图片明细数量",
    "hint": "超过该数量时只显示省略计数。"
  },
  "ref_preview_chars": {
    "type": "int",
    "default": 96,
    "description": "路径或 URL 预览长度",
    "hint": "只影响日志展示，不影响统计。"
  },
  "remember_last_sessions": {
    "type": "int",
    "default": 50,
    "description": "保留最近多少个会话的审计摘要",
    "hint": "用于 /image_audit_status 命令查询当前会话最近一次审计结果。"
  }
}
